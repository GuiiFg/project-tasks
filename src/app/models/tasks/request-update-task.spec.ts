import { RequestUpdateTask } from './request-update-task';

describe('RequestUpdateTask', () => {
  it('should create an instance', () => {
    expect(new RequestUpdateTask()).toBeTruthy();
  });
});
